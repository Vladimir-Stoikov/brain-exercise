import styled from 'styled-components';

export const TypingLayout = styled.section`
  display: flex;
  flex-direction: column;
  gap: 1.5rem;

  padding: 2rem;

  width: fit-content;
  max-width: 100%;

  background-color: var(--primary);

  border-radius: 1rem;

  box-shadow: 0 0.5rem 1.5rem rgba(0, 0, 0, 0.3);
`;