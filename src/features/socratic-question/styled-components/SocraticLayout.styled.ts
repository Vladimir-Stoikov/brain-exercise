import styled from 'styled-components';

export const SocraticLayout = styled.section`
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 24px;

  width: min(800px, 100%);
  margin: 0 auto;
`;