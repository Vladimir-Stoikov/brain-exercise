import styled from 'styled-components';

export const StroopLayout = styled.section`
  display: flex;
  flex-direction: column;
  gap: 1.5rem;

  padding: 2rem;
  min-width: 320px;
  max-width: 420px;

  background-color: var(--primary);
  border-radius: 1rem;
  box-shadow: 0 0.5rem 1.5rem rgba(0, 0, 0, 0.3);
`;
